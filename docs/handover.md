# Skralli.is rebuild — handover log

Kept up to date by Claude during the rebuild. Items move to "Done" when closed.
Branch: `site-recreation`, PR: https://github.com/kode-is/skralli/pull/1

## Still needs you or Hlynur

1. **Resend**: add `RESEND_API_KEY`, `EMAIL_FROM`, `CONTACT_TO` in Vercel (and `.env.local` locally if you want to test here), then send one real form submission.
2. **Floor-mat price**: live shows 21.700 kr on desktop and 16.800 kr on mobile; the new site mirrors that until one is chosen.
3. **Alt texts**: 17 rewritten in Icelandic, listed in the commit "Replace stock-template alt text and add site-wide og:image", for Hlynur to review.
4. **Drive photos**: drop the "Skralli x Kode myndir fyrir netsíðu" folder zip in Downloads whenever convenient and Claude will run the higher-resolution swap.
5. **Deploy**: the repo is linked to the Vercel project `skralli` under Kode Solutions. Preview of the PR branch (login-protected): https://skralli-n77csl614-kode-solutions-44807b0f.vercel.app . Because Vercel promotes a project's first deployment to production, https://skralli.vercel.app is also live and public; it is a copy of the first deployment, carries canonical links to skralli.is, and only gets the new no-index header on the next production deploy. Decide: keep it as a share link for Hlynur, redeploy production so it is no-indexed, or lock it behind Vercel login. Still to do: add `RESEND_API_KEY`, `EMAIL_FROM`, `CONTACT_TO` in Vercel (Project > Settings > Environment Variables) or the forms fail on Vercel; connect the GitHub repo for automatic PR previews; production deploy after merge; point the skralli.is domain at Vercel (separate DNS step).
6. **Placeholder table**: the live Hífikeðjur page has a leftover Framer template table (Name / Email / Role / Status with fictional people). It is not reproduced; tell Hlynur so it is not expected.
7. **Breadcrumb bug on live**: the live Bindikeðjur og strekkjarar page's breadcrumb reads "Hífikeðjur"; the new site shows the correct title. No action unless you want the bug kept.
8. **Design proposals**: review the mockups for ideas 5 (spec tables) and 6 (sub-page skeleton) and say which to build.
10. **Breadcrumb rollout**: the hero-edge breadcrumb from the Claude Design product page (project "Breadcrumb design exploration") is live on the 25 product pages and the 5 wagon-group pages only. Say the word to roll it out to the other 13 pages that still use the plain grey trail (category, brand, contact and about pages).
9. **Merge**: when the PR looks right, merge it (or ask Claude to). `AGENTS.md` / `CLAUDE.md` in the repo root were auto-generated by Next.js dev; commit or gitignore them as you prefer.

## Post-launch suggestions (no decision needed now)

- Downscale the full-resolution originals in `public/images` (128 MB) to about 2560 px; keep the sources elsewhere.
- Rate-limit the contact action (Vercel Firewall or a per-IP throttle); the honeypot is the only bot defence today.
- Unique title and description per category and brand page (21 routes share the site title).
- Serve the hero video only on desktop, or a shorter compressed loop.
- The Bindikeðjur "Bindikeðjur" section on live lays two ranges side by side; the new site stacks them.
- Layout pass: a few sections still differ from live in inner spacing (e.g. the Skóbakkar block on Fyrir ökumanninn: live indents the text column and uses a 36 px heading). Say the word and Claude will run the same page-by-page pixel comparison used for the dot bands and category cards.

## Done

- 2026-09-17 Footer: 2026 Fyrirmyndarfyrirtæki badge replaces the 2025 one. The footer logo was rendering as a dotted ghost because the four logo PNGs transcribed from the Drive connector on day one were corrupt (bad checksums, truncated); the footer now uses the live site's own blue logo at the live 178 px width and the corrupt files are removed. If Hlynur's Drive logo originals are wanted later, take them from the downloaded zip (item 4), not through the connector.
- 2026-09-16 Motion and hover polish (from the skralli-v2 comparison): sections and card grids fade up on scroll with a stagger, hero text fades in on load, the Þjónusta and phone menus animate open, cards lift with an arrow nudge on hover, FAQ answers animate, the brand marquee pauses on hover, smooth scrolling. Built on the `motion` package with reduced-motion honoured; a before/after layout snapshot shows no element moved. Also: brand-strip logos now match the live per-logo boxes at every width, and the phone menu has a single close cross like live.
- 2026-09-10 Product pages rebuilt from the Claude Design mockup (https://claude.ai/design/p/dc78159c-cb02-41d7-9046-b663634948ae): breadcrumb in the hero's bottom edge, fact strip (size/category/maker/origin/warranty/tipping angle, only where the data has it), sticky quote card with labelled fields and inline errors, numbered feature tiles, whole-group sibling cards with the current product marked, and on phones a collapsed quote card plus a sticky "Fá tilboð" bar. Copy is unchanged; the only new strings are the design's labels.
- 2026-09-08 Dot bands redrawn to the live treatment (48 px tile, opacity 0.1, 100/75 px tall, flush with the next section) on all five pages; the sections that follow now start at the live offsets.
- 2026-09-08 Home category cards rebuilt to the live geometry; the icon badge sits in front of the card body again, straddling the image edge.
- 2026-09-08 Fonts switched to the live site's Figtree / Inter / Satoshi (self-hosted), replacing NowAlt at Einar's request; every family/weight was measured on the live pages and applied per element.
- 2026-09-08 Header: "Þjónusta" hover menu, clean logo at live size, nav beside the button, mobile menu as floating card (PR commits `0de29f2`…`135eb3e`).
- 2026-09-08 Page container widened to the live 1240 px content width.
- 2026-09-08 Empty product pages filled in: all 25 Sturtuvagnar product pages now built from mockup 1 (design/Main.tpl.html) — product blurb + group "Um X" copy, the two Gigant/warranty pills, an inquiry card that prefills the message with the product name, the seven feature tiles (hjólagröfuvagnar products only), and linked sibling-product cards. The group pages' own "Tegundir í boði" cards are now clickable through to these pages too. No new copy was written — everything comes from the existing scrape data.
