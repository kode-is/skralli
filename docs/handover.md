# Skralli.is rebuild — handover log

Kept up to date by Claude during the rebuild. Items move to "Done" when closed.
Branch: `site-recreation`, PR: https://github.com/kode-is/skralli/pull/1

## Still needs you or Hlynur

1. **Resend**: add `RESEND_API_KEY`, `EMAIL_FROM`, `CONTACT_TO` in Vercel (and `.env.local` locally if you want to test here), then send one real form submission.
2. **Floor-mat price**: live shows 21.700 kr on desktop and 16.800 kr on mobile; the new site mirrors that until one is chosen.
3. **Alt texts**: 17 rewritten in Icelandic, listed in the commit "Replace stock-template alt text and add site-wide og:image", for Hlynur to review.
4. **Drive photos**: drop the "Skralli x Kode myndir fyrir netsíðu" folder zip in Downloads whenever convenient and Claude will run the higher-resolution swap.
5. **Deploy**: the Vercel link and production deploy are not done; say the word and Claude will link the project under the Kode team and deploy the PR branch as a preview, then production once merged. Pointing the skralli.is domain at Vercel is a separate DNS step for when you're ready.
6. **Placeholder table**: the live Hífikeðjur page has a leftover Framer template table (Name / Email / Role / Status with fictional people). It is not reproduced; tell Hlynur so it is not expected.
7. **Breadcrumb bug on live**: the live Bindikeðjur og strekkjarar page's breadcrumb reads "Hífikeðjur"; the new site shows the correct title. No action unless you want the bug kept.
8. **Design proposals**: review the mockups for ideas 5 (spec tables) and 6 (sub-page skeleton) and say which to build.
9. **Merge**: when the PR looks right, merge it (or ask Claude to). `AGENTS.md` / `CLAUDE.md` in the repo root were auto-generated by Next.js dev; commit or gitignore them as you prefer.

## Post-launch suggestions (no decision needed now)

- Downscale the full-resolution originals in `public/images` (128 MB) to about 2560 px; keep the sources elsewhere.
- Rate-limit the contact action (Vercel Firewall or a per-IP throttle); the honeypot is the only bot defence today.
- Unique title and description per category and brand page (21 routes share the site title).
- Serve the hero video only on desktop, or a shorter compressed loop.
- The Bindikeðjur "Bindikeðjur" section on live lays two ranges side by side; the new site stacks them.

## Done

- 2026-09-08 Header: "Þjónusta" hover menu, clean logo at live size, nav beside the button, mobile menu as floating card (PR commits `0de29f2`…`135eb3e`).
- 2026-09-08 Page container widened to the live 1240 px content width (pending push).
- 2026-09-08 Empty product pages filled in: all 25 Sturtuvagnar product pages now built from mockup 1 (design/Main.tpl.html) — product blurb + group "Um X" copy, the two Gigant/warranty pills, an inquiry card that prefills the message with the product name, the seven feature tiles (hjólagröfuvagnar products only), and linked sibling-product cards. The group pages' own "Tegundir í boði" cards are now clickable through to these pages too. No new copy was written — everything comes from the existing scrape data.
